require 'rails_helper'

describe HangoutsController do
  describe '#show' do
    # it 'sets the current user as the sender' do
    #   get '/hangout?user_id=2'
    #
    #   sender = assigns(:sender)
    #
    #   expect(sender).to eq({})
    # end
  end
end